"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var extractLinkFunc = function extractLinkFunc(data) {
  data = data.toString();
  var matchIndex = data.search(/\s*http(s)?:\/\//);
  var result = { data: data };
  if (matchIndex !== -1) {
    result.data = data.substr(0, matchIndex);
    result.link = data.substr(matchIndex).trim();
  }

  return result;
};

exports.extractLinkFunc = extractLinkFunc;